# Host port for the web service. Change if 5173 is already in use.
HOST_PORT=5173
# Optional: bind container port to a specific host IP. Change if you have
# configured an extra IP on your machine (e.g. 126.12.21.1). Defaults to 0.0.0.0
# To use e.g. 126.12.21.1, set HOST_IP=126.12.21.1 and ensure that IP is assigned
# to a local interface before starting Docker.
HOST_IP=0.0.0.0
